# 待完成任务

## 高优先级

- [x] 实地反馈“转向太大错过出口”：已修正为行驶中转向受油门约束（内轮不反转）、原地转向仅限
      停车时且幅度温和（0.60）。详见 DECISIONS 的 D003。
- [ ] 窄开口对准（<0.6 米）：仿真实测开口 ≥0.6 米可靠通过，0.5 米会碰撞，因为当前无显式路径生成。
      需要 follow-the-gap / VFH+ 决策层才能在更窄的开口里对准。
- [ ] 开阔场地续航复测：在正前方至少 3 米无遮挡处验证持续前进，并标定减速阈值。
- [ ] `ground_avoidance`：细障碍物（细柱、桌腿）识别。碰撞判断和间隙宽度目前共用 20 百分位数，
      细障碍物占扇区比例低于约 20% 时检测不到。计划改为"扇区最小值做碰撞判断 + 百分位做间隙宽度"两套指标。
- [ ] `ground_avoidance`：死角 / U 型死胡同脱困。目标力恒指向相机正前方且不自动倒车，
      进入封闭死角后可能原地摆动。计划加卡死检测（推力为零、转向饱和持续 N 帧且位移很小）触发一次有界倒车转向。
- [x] 真实硬件验证安全修复：已在实车观察到连续绕障行驶（见 HANDOFF 实车复测），
      并修正了实地反馈的过量转向问题。距离阈值仍待开阔场地标定。

## 中优先级

- [ ] `vlm_navigation`：真车验证与现场标定。目前只完成离线逻辑测试与假模型链路验证，
      需要在拆桨/架空的条件下用 `--fake-vlm --hardware` 确认相机与串口，再用真实 VLM 跑。
      没有里程计，`seconds × speed` 是开环量，需按实际地面摩擦标定 `max_action_seconds` 与 `max_speed`。
- [ ] `vlm_navigation`：确认模型同时支持图像输入与 function calling。若所选模型/服务商不支持，
      需要改 prompt 或换成支持工具调用的模型；当前失败会返回明确 HTTP 错误并停车。
- [ ] `vlm_navigation`：`max_action_seconds` 与动作粒度调优。目前单次动作上限 2 秒，
      决策频率受公网 API 延迟限制，起步/绕障的平顺度需要现场调参。
- [ ] `vlm_navigation`：成本与延迟观测。每轮上传一张图并等待模型回复，长时间运行的费用与
      往返延迟尚无实测数据；上下文已限制为最近 2 张图片。
- [ ] 深度帧离线回放调参工具：录制深度帧为 `.npy`，编写回放脚本离线扫参数，避免每改一个增益就上真车。
- [x] `ground_avoidance` 地面剔除 / ROI 标定：已实现解析地面模型 `s=K/(y-horizon)` 并现场标定
      （horizon=141.7、K=44.5），新增 `calibrate_ground.py`。详见 DECISIONS 的 D002。
- [ ] 里程/位移反馈：小车没有里程计，远程测试无法确认实际位移，只能确认指令已发出。
      如需闭环验证或死角卡死检测，需要引入里程信息（编码器或视觉里程计）。
- [ ] 地面剔除的姿态补偿：当前地面模型假设水平平地，上下坡或大幅俯仰会失准。
      可考虑在自主运行时用 PX4 姿态角修正 horizon，或在不平地形下退化为固定 ROI。
- [ ] `run_ground_avoidance.py` 增加看门狗：standalone 运行时若深度读取失败或阻塞，应自动停车
      （网络控制模块已由 0.5 秒地面看门狗间接覆盖，standalone 模式没有）。
- [ ] 运动学仿真的定期回归：本轮用 `/tmp` 下的临时仿真脚本验证了出口通过性，值得把它整理进
      `tests/`，作为“能穿过开口”的自动化回归，避免以后再引入类似 D003 的过量转向回归。

## 机械外壳（mechanical/car_shell）

- [ ] 按实物复核推定尺寸并重新生成：`WHEEL_WIDTH`(25)、`MOTOR_DIA`(36)、`MOTOR_LEN`(59)、
      `SHAFT_HOLE_R`(6)、`JETSON`(105×85×35，载板实际 100×79)，以及从动轮底座外形
      `CASTER_PLATE_X/Y`(34×42)、板厚(3)、轮径(`CASTER_WHEEL_R` 10)、拖尾(`CASTER_TRAIL` 5)。
      后轮孔距 23×30、底座高 33 已由用户给定并纳入校核，无需再核。
- [ ] 从动轮 360° 旋转实测：几何上底座以下部件全在车底之下、不干涉，但拖尾与轮径
      是推定值；实物装上后需确认旋转到任意角度都不碰车底与地面。
- [ ] 滑槽底板加厚：当前顶板 2.3 mm（由总高 72 与滑盖厚度 2 决定）。若需达到任务书
      提到的 3~3.5 mm 局部加厚，把 `BAY_Z_TOP` 从 87.5 降到约 86.6（舱内净高仍余 1.1 mm）。
- [ ] 首件试打印建议只打滑盖：验证 170×105×2 与滑槽 0.2/0.5 mm 配合是否顺畅，
      再打印主壳体（主壳体打印耗时长、失败代价高）。
- [ ] 从动轮轮架设计：当前只在车尾留 Ø9 安装孔与两侧 M3 螺孔，未设计轮架本体。
- [ ] Jetson 固定方案细化：当前为 4 个 M3 自攻底孔，未确定铜柱高度与螺柱选型。
- [ ] 强度校核：轮罩与车体连接处、顶部大面积薄板未做受力/模态分析。
- [ ] 滑盖运动仿真：当前"可完全抽出"是几何可用性推断（滑槽尾端开放无阻挡），
      未做运动学仿真。

## 低优先级

- [ ] 清理仓库中的补丁残留备份文件：`potential_field.py.orig`、`static/app.js.orig`、
      `tests/test_potential_field.py.orig`（已被 `.gitignore` 忽略，不影响运行）。

### 机械外壳补记（D007 后）

- [ ] 从动轮固定方式实测：当前为底板+安装垫共 7.5 mm 受力厚度 + Ø3.4 通孔，
      计划用 M3 × 14 螺丝。实物装配时确认：螺丝能从下方穿过底座与垫子、锁紧后不顶到
      垫子反面；若不打算用螺母，可改用 M3 热熔铜螺母嵌入 5 mm 厚的垫子内。
- [ ] 从动轮位置可再调：当前底座中心 x=-122（D008 后），旋转包络距车尾 15.5 mm。
      受安装台与开口范围约束，前移空间有限。
- [ ] 车尾掏空后的强度复核：尾壁/底板/顶盖板与安装台共同承力，安装台需承受从动轮
      全部载荷。未做受力分析，建议打印后按压车尾确认刚度，必要时加厚安装台或加筋。

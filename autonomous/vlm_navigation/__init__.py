"""CONTROL VLM 视觉导航子模块：拍照 → VLM 决策 → 地面运动。

本模块与 `ground_avoidance` 平级且相互独立，只负责地面车，不涉及 PX4 或飞行。

模块内部统一使用“扁平导入”（直接 `import motion` 这类同目录模块名），与
`ground_avoidance` 的做法一致，因此这里不再用相对导入重复导出，避免出现
“既能当包又能当目录”两种导入方式互相冲突的情况。
"""
